<ul class="nav nav-pills nav-fill">
    <li class="nav-item">
        @Html.ActionLink("Genreal Information", "Details", new { id = Convert.ToString(Session["TempId"]) }, null)

    </li>
    @*<li class="nav-item">
            <a class="nav-link" href="#">Settings</a>
        </li>*@
    <li class="nav-item">
        @Html.ActionLink("Settings", "ChangePassword", new { id = Convert.ToString(Session["TempId"]) }, null)

    </li>
    <li class="nav-item">
        @Html.ActionLink("Document", "Documents", new { id = Convert.ToString(Session["TempId"]) }, null)

    </li>
    <li class="nav-item">
        @Html.ActionLink("History", "MyHistory", new { id = Convert.ToString(Session["TempId"]) }, null)

    </li>
    @if (!string.IsNullOrEmpty(Convert.ToString(Session["Role"])) && Convert.ToInt32(Session["Role"]) == (int)webIEA.Dtos.IEARoles.Admin)
    {
        <li class="nav-item">
            @Html.ActionLink("Member List", "IndexPage")
        </li>
        if (Convert.ToString(Session["TempId"]) != Convert.ToString(Session["loginUserId"]))
        {
            <li class="nav-item">
                @Html.ActionLink("Delete", "DeleteMember", new { id = Convert.ToString(Session["TempId"]) }, null)
            </li>
            <li class="nav-item">
                @Html.ActionLink("Suspend", "SuspendMember", new { id = Convert.ToString(Session["TempId"]) }, null)
            </li>
        }
    }
</ul>